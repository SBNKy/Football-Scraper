[project]
name = "footballscraper"
version = "0.1.0"
description = "Application to fetch football data from top 5 leagues in Europe"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32.4",
]
